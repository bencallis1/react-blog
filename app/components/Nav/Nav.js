import React, {PropTypes} from 'react';
import { Link } from 'react-router';
require("./Nav.css");

const Nav = () => {
    return (
        <div className="nav-main-container">
            <nav>

            </nav>
        </div>
    );
}

export default Nav